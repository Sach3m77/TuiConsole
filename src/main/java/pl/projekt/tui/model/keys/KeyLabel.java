package pl.projekt.tui.model.keys;

/**
 * Enum class representing keyboard keys.
 * Each enum value corresponds to a specific key on the keyboard.
 */
public enum KeyLabel {
    /** Up arrow key. */
    ARROW_UP,
    /** Down arrow key. */
    ARROW_DOWN,
    /** Right arrow key. */
    ARROW_RIGHT,
    /** Left arrow key. */
    ARROW_LEFT,
    /** F1 key. */
    F1,
    /** F2 key. */
    F2,
    /** F3 key. */
    F3,
    /** F4 key. */
    F4,
    /** F5 key. */
    F5,
    /** F6 key. */
    F6,
    /** F7 key. */
    F7,
    /** F8 key. */
    F8,
    /** F9 key. */
    F9,
    /** F10 key. */
    F10,
    /** F11 key. */
    F11,
    /** F12 key. */
    F12,
    /** Digit 0 key. */
    DIGIT_0,
    /** Digit 1 key. */
    DIGIT_1,
    /** Digit 2 key. */
    DIGIT_2,
    /** Digit 3 key. */
    DIGIT_3,
    /** Digit 4 key. */
    DIGIT_4,
    /** Digit 5 key. */
    DIGIT_5,
    /** Digit 6 key. */
    DIGIT_6,
    /** Digit 7 key. */
    DIGIT_7,
    /** Digit 8 key. */
    DIGIT_8,
    /** Digit 9 key. */
    DIGIT_9,
    /** Comma key. */
    COMMA,
    /** Period key. */
    PERIOD,
    /** Enter key. */
    ENTER,
    /** Alternative Enter key. */
    ENTER_ALT,
    /** CTRL + C key combination. */
    CTRL_C,
    /** Delete key. */
    DELETE,
    /** Escape key. */
    ESC,
    /** Unknown key. */
    UNKNOWN,
    /**
     * Special key for internal window resizing.
     */
    INTERNAL_WIN_RESIZE,
    /** Space key. */
    SPACE
}
